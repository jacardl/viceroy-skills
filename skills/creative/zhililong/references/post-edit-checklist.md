# zhililong · 事后清单（11 项硬约束）

写完一篇 zhililong 长文后，**必跑**这 11 项检查。任一不通过 → 回改。

## 必查 11 项

| # | 检查项 | 通过标准 | 怎么查 |
|---|--------|----------|--------|
| 1 | "不是 X 而是 Y" 句式 | 0 处 | `grep "不是.*而是" article.md` 应返回空 |
| 2 | 排比三连 | 0 处 | 全文搜 "、"，连续 3 个逗号在同一段 = 排比 |
| 3 | `——` 破折号 | 0 处 | `grep "——" article.md` 应返回空 |
| 4 | 段落级加粗 | 区分两类：①段落级加粗 = 形容词/副词/修饰性加粗，每节最多 1 处；②论点关键词加粗 = 章节核心术语 / 节内并列论点（如"出口/人流/钱流"三并列结构），每节最多 3 处 | 手动数 `**` 数量 → 区分两类分别计数；判断口诀见下方「判断粒度」小节 |
| 5 | AI 套话 | 0 处"在这个快节奏的时代"/"让我们"/"赋能" | `grep -E "(在这个.*时代|赋能|让我们一起)"` 应返回空 |
| 6 | 意义拔高 | 0 处"这不仅是 X，更是 Y" | `grep "这不仅.*更是"` 应返回空 |
| 7 | 万能展望结尾 | 0 处"未来属于"/"终将" | 末段手检 |
| 8 | 谄媚/夸赞 | 0 处"你真棒"/"作为 X 怎能不" | `grep -E "(你真棒|怎能不|太.*了)"` 应返回空 |
| 9 | emoji | 0 处 | `grep -E "[\U0001F300-\U0001F9FF]"` 应返回空 |
| 10 | 填充对冲 | 0 处"当然也不排除"/"可能因人而异" | 末两段手检 |
| 11 | 字数 | 4000-5500 中文字符 | `python3 -c "import re; print(len(re.sub(r'[^\u4e00-\u9fa5]', '', open('article.md').read())))"` |

## 自检结果输出格式

```markdown
## 自检

- [x] 不是 X 而是 Y：0 处
- [x] 排比三连：0 处
- [x] 破折号：0 处
- [x] 段落级加粗：段落级 0 处；论点关键词 3 处（出口/人流/钱流，均为 H2「二」并列论点）
- [x] AI 套话：0 处
- [x] 意义拔高：0 处
- [x] 万能展望结尾：0 处
- [x] 谄媚/夸赞：0 处
- [x] emoji：0 处
- [x] 填充对冲：0 处
- [x] 字数：5321 中文字符
```

**自检清单必须在交付时附给用户**——这是 zhililong 区别于普通"写文章"的硬证据。

## ⚠️ 清单扫描的"作用域"边界（2026-06-14 实战沉淀）

清单的 11 项**只扫正文**（即 `## 一、` ~ `## 反转：` / `## 行动：` 之间的部分），不扫：

- **markdown 头部元信息**：`# 标题`、`**字数**`、`**结构**`、`**自检**` 等
- **`## 大纲`** 节（不是文章内容）
- **`## 参考资料`** 节（不是文章内容）

**实现方式**：自检脚本读取 markdown 后，用正则 `re.search(r"## 一、.*?(?=\n## 参考资料)", src, re.DOTALL)` 抽出正文，再跑 11 项检查。如果不切片，markdown 头部的 `**字数**` 等加粗元信息会被算成"段落级加粗"误报。

## ⚠️ 第 4 项「段落级加粗」的判断粒度（2026-06-14 实战沉淀）

「每节最多 1 处关键词加粗」这条规则，**不适用于**：

- H2 节内**多个并列论点**的核心关键词（典型场景：分论点用"第一/第二/第三"列举，每条都加粗 1 个论点词）
- 案例：用 `**出口**` / `**人流**` / `**钱流**` 三个并列论点词标注一节内的三段分论

**适用于**：

- 大段铺陈的形容词/副词加粗（如"**极其重要**"这种修饰性加粗）
- 同一段内连续 2 个以上的加粗词

**判断口诀**：加粗的词是不是"读者扫一眼就能定位这一段在讲什么"的那种关键词？是 → 允许；不是 → 砍。

如果出现判断两可的情况，**在交付时显式说明**（如「正文论点加粗 3 处：出口 / 人流 / 钱流，均为 H2「二」一节内并列论点关键词，非段落加粗」），让用户审稿时定夺。

## ⚠️ 排比三连检测的已知局限性（2026-07-10 实战沉淀）

`check_parallel_three()` 检测的是「连续 4 项被 `、` 分隔」的文本模式，无法区分：

- **修辞性排比三连**（如「勤奋、勇敢、善良」）→ 应消除
- **事实性编号列举**（如「第一条、第二条、第三条」）→ 应保留

实操经验（2026-07-10）：陈浩 VIE 长文里原始写法是「第一，要...。第二，要...。第三，要...。」带句号分隔，脚本报 OK；但早期草稿用了「第一条...第二条...第三条...」且中间有顿号分隔，触发了误报。**改版原则**：编号诉求列表不用顿号分隔，改用句号独立成段+换行，即可规避误报，同时保持可读性。

**判断口诀**：编号列举（第一/第二/第三）用句号断句；修辞排比（「X、Y、Z」）才需要拆散重写。

---

## 实战案例（冰鉴长文自检结果）

```
- 不是 X 而是 Y: 0
- 排比三连: 0
- 破折号: 0
- 段落级加粗: 1（仅"协调"，作为论点关键词保留）
- AI 套话: 0
- 意义拔高: 0
- 万能展望结尾: 0
- 谄媚: 0
- emoji: 0
- 填充对冲: 0
- 字数: 5305 中文字符（含标点 5576）
```
