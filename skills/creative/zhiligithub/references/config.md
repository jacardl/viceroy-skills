# 直隶按察使 · 公众号凭证配置
# ⚠️ 此文件仅放占位符；真实凭证在 `~/.openclaw/secrets/zhili-credentials.md`（不进 GitHub）
# `publish_zhili.py` 会优先读本文件；若本文件无真实密钥，回退到 `~/.openclaw/secrets/zhili-credentials.md`

APPID: wx38a91c353554588a
APPSECRET: ***REDACTED***  # 真实值见本地 secrets 文件
CATEGORY_ID: 100

# IP 白名单（服务器出口 IP，需在微信后台配置）
# 当前服务器 IP 段：39.102.x.x
# 建议添加整个 39.0.0.0/8 网段
